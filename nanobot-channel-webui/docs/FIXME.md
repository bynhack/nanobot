# 修复清单

本文件用于跟踪已经确认的问题、风险和技术债。任何代码修复前应先确认本清单是否已有对应条目；没有则新增，修复完成后更新状态、证据和后续项。

## 状态约定

- `待处理`：已确认，需要修复。
- `进行中`：当前正在处理。
- `已修复`：代码已修改并通过必要验证。
- `延后`：暂不处理，需要说明原因。
- `取消`：确认不再需要处理，需要说明原因。

## 优先级约定

- `P0`：安全边界、数据泄露、权限绕过、服务不可用，必须优先处理。
- `P1`：稳定性、重要流程一致性、可维护性问题，近期处理。
- `P2`：整理、重构、文档同步、体验优化，可排期处理。

## 当前修复项

| ID | 优先级 | 状态 | 问题 | 涉及位置 | 验收标准 | 备注 |
|---|---|---|---|---|---|---|
| FIX-001 | P0 | 已修复 | scoped 用户下通用工具层存在默认放行链路，`read_file`、`list_dir`、`exec` 和未知工具的边界需要重新收口，同时保留普通办公技能自由组合能力。 | `src/nanobot_channel_webui/permissions/tool_registry.py`、`src/nanobot_channel_webui/permissions/command_policy.py` | scoped 用户不能读取敏感文件、枚举敏感目录或通过 shell 绕过受控业务 CLI；Word、Excel、浏览器等普通技能仍可基于授权 artifacts 正常使用。 | 已发布验证：权限测试通过；敏感 `read_file/list_dir/exec` 被拒绝，普通 `create_docx` 透传。 |
| FIX-002 | P0 | 已修复 | 敏感路径保护不完整，`~/.nanobot/config.json`、sessions JSONL、audit、private、policies、配置文件等需要纳入读取/列目录边界。 | `src/nanobot_channel_webui/permissions/tool_registry.py` | scoped 用户无法通过 `read_file` 或 `list_dir` 获取敏感配置、会话历史、权限文件、审计文件和私有运行时文件。 | 已发布验证：`~/.nanobot/config.json`、workspace root、sessions/audit/private/policies 均被拒绝。 |
| FIX-003 | P0 | 已修复 | 权限上下文存在 fail-open 默认值：无用户、缺 role、缺 policy file、缺 `NANOBOT_WEBUI_POLICY_FILE` 时可能退化为 admin/unrestricted。 | `src/nanobot_channel_webui/permissions/context.py`、`src/nanobot_channel_webui/permissions/resolver.py`、`src/nanobot_channel_webui/permissions/command_policy.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py` | scoped 或身份缺失场景默认拒绝受控业务访问；只有显式 admin policy 才 unrestricted；已认证 admin 保持 unrestricted。 | 已发布验证：fail-closed 默认值保持不变；`resolve(None)` 非特权；已认证 admin 分支显式构造 `role=admin/business_role=admin`，回归测试 `test_policy_resolver_admin_is_unrestricted` 已覆盖；`./scripts/publish-local.sh` 通过。 |
| FIX-004 | P0 | 已修复 | 身份证号和手机号查询没有强制 company scope，可能导致跨公司 PII 泄露。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 使用身份证号或手机号查询时，无论是否显式传 `--company`，结果都必须限制在当前账号授权公司范围内。 | 已修复：证件号和手机号查询统一下推 `companyName/companyNames`，显式公司也会校验 scope。 |
| FIX-005 | P1 | 已修复 | 权限运行时注入失败时可能静默降级，scoped 请求仍继续执行。 | `src/nanobot_channel_webui/permissions/injector.py`、`src/nanobot_channel_webui/channel.py` | scoped 请求在权限 runtime 未 attached 时硬失败；管理端观测信息能明确显示注入状态。 | 已修复：tenant runtime 注入失败时 runtime 标记失败，scoped message/cancel 请求直接拒绝。 |
| FIX-006 | P1 | 已修复 | 写入 scope 只在 CLI 入口扫描 JSON plan，repository 最终写入层不感知授权 scope。 | `src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py` | 写入、删除、验证在 repository 层也能基于授权公司范围做最终校验。 | 已修复：repository 写入、删除、验证入口统一调用 plan 公司范围兜底校验；CLI 入口仍保留第一道权限校验。 |
| FIX-007 | P1 | 已修复 | scoped 用户的工具定义仍可能暴露完整工具表，和实际权限边界不一致。 | `src/nanobot_channel_webui/permissions/tool_registry.py` | 模型看到的工具定义和 scoped 用户可执行能力一致；普通办公工具保留，敏感工具或敏感参数面不暴露。 | 已修复：scoped 工具定义隐藏 `grep/edit_file/spawn/my/cron/long_task/notebook_edit` 等敏感运行时工具；普通办公类工具继续透传。 |
| FIX-008 | P1 | 已修复 | `business delete` 流程没有和 create/update 一样统一补内部确认语义，自然语言删除流程不稳定。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 删除类 business 命令走统一 preview/确认/执行/验证流程，用户不需要理解底层 `--confirm`。 | 已修复：`business delete employee` 自动补内部确认语义。 |
| FIX-009 | P2 | 已修复 | HR 任务聚焦词硬编码在通用权限层，影响多业务域通用性。 | `src/nanobot_channel_webui/permissions/skill_view.py`、各业务 `tenant-runtime.json` | 业务术语、focus terms 和 related capabilities 由业务契约声明，通用运行时只读取契约。 | 已修复：新增 capability `focus_terms` 契约字段，HR 词表迁入 HR `tenant-runtime.json`，通用动态视图渲染器不再硬编码 HR 词表。 |
| FIX-010 | P2 | 已修复 | `permissions` 与 `tenant_runtime` 双模块命名和再导出较多，长期维护成本高。 | `src/nanobot_channel_webui/permissions/`、`src/nanobot_channel_webui/tenant_runtime/` | 明确主模块边界，减少重复导出和兼容别名。 | 已修复：明确 `tenant_runtime` 为新公共命名层，`permissions` 为当前实现和兼容导入层；不做破坏性删除，避免影响已有 import。 |
| FIX-011 | P2 | 已修复 | `TenantGuard` 当前是旁路 SDK，有测试和导出，但未进入 HR 主执行链路，文档表述容易误导。 | `src/nanobot_channel_webui/tenant_runtime/guard.py`、`docs/` | 文档准确描述当前 enforcement 主链路；若保留 SDK，要说明适用场景。 | 已修复：权限硬化文档已明确 HR 主链路由命令网关、动态技能视图、HR CLI 和 repository scope 兜底共同完成；`TenantGuard` 是新业务技能可复用 SDK。 |
| FIX-012 | P2 | 已修复 | 存在重复 JS guard 文件和 `__pycache__`、旧 `case_graph` 缓存残留。 | `src/nanobot_channel_webui/tenant_runtime/*.mjs`、`src/**/__pycache__`、`src/nanobot_channel_webui/case_graph/` | 重复文件和缓存残留清理完成，打包产物不包含无关缓存。 | 已修复：删除重复 `tenant_runtime_guard.mjs`，清理缓存，并在发布验证脚本编译后自动删除 `__pycache__`。 |
| FIX-013 | P2 | 已修复 | help 输出仍混有 legacy 命令和 business 命令，容易让模型执行绕路。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 面向智能体的 help 只暴露标准 business surface；legacy 命令仅作为内部兼容 alias。 | 已修复：默认 `help` 输出标准 business surface；legacy 命令保留为兼容 alias 和特定命令帮助。 |
| FIX-014 | P0 | 已修复 | HR CLI 直调 `count-all` 必须走业务 runtime 授权，不能只依赖 Python exec 网关的 `denied_commands` 兜底。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`tests/test_permissions.py` | scoped 用户直调 `nanobot-webui-business hr count-all` 时被 Python policy 拒绝；拒绝发生在数据库访问前；admin/unrestricted 仍可执行全局统计。 | 已修复并补回归测试：`count-all` case 调用 `authorize_hr_command`，命中 `SCOPED_GLOBAL_DENY_COMMANDS`；`test_hr_cli_count_all_is_denied_by_python_policy_for_scoped_user` 覆盖 CLI 直调。 |
| FIX-015 | P1 | 已修复 | assistant-ui runtime 在同一条消息内收到重复 `toolCallId` 时触发 `Duplicate key ... in tapResources`，导致页面崩溃。 | `frontend/src/app-helpers.ts`、`frontend/src/app-helpers.test.ts` | 重复上游 `callId` 不会生成重复 assistant-ui `toolCallId`；页面保持可渲染，工具调用仍可展示。 | 已修复：assistant-ui 投影层对同一消息内重复 `toolCallId` 稳定消歧；验证：新增回归测试失败后修复，前端定向测试通过，`./scripts/publish-local.sh` 通过并已重启 gateway。 |
| FIX-016 | P1 | 已修复 | 托管实例模式删除会话时 `/api/upstream/sessions/{chat_id}` 返回 502，浏览器显示“删除会话失败”。 | `src/nanobot_channel_webui/channel.py`、`tests/test_channel_managed_instances.py` | 浏览器仍可向 WebUI 插件发 `DELETE`；插件代理到 Nanobot websocket sidecar 时使用其支持的 `GET /api/sessions/{key}/delete`；删除请求不触发 websocket HTTP parser 错误。 | 已修复：根因是 Nanobot websocket sidecar 基于 `websockets`，只接受 HTTP `GET`；内部代理恢复 GET 并补回归测试。验证：`tests/test_channel_managed_instances.py` 和全量 Python 测试通过。 |
| FIX-017 | P1 | 已修复 | 多实例 HR 查询存在执行绕路：实例 CLI 没有自动带入 policy 文件，模型首次调用失败后会自行查找敏感运行时路径；公司和部门查询被拆成 N+1 次 CLI。 | `src/nanobot_channel_webui/business_modules/hr/cli.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_business_cli.py`、`tests/test_permissions.py` | 实例 workspace 下运行 `nanobot-webui-business hr ...` 自动发现 `.nanobot_channel_webui/policies/policy.json`；“可见公司及部门”可通过单条 `business query organization-tree` 完成；权限网关仍识别该命令并注入 policy env。 | 已修复：新增 policy 自动注入、`organization-tree` 聚合命令、HR 契约 capability 和短路由说明；验证：新增红绿回归测试和权限网关定向测试通过。 |
| FIX-018 | P1 | 已修复 | 旧 WebUI channel 本地对话逻辑仍保留在主代码中：`/ws`、外层 session 存储、前端 fallback 协议、外层 AgentLoop hook 注入等与多实例 upstream 主链路重叠。 | `src/nanobot_channel_webui/channel.py`、`frontend/src/ws-client.ts`、`frontend/src/use-websocket-session.ts`、`frontend/src/api.ts`、`src/nanobot_channel_webui/permissions/` | 在确认 upstream-only 产品边界、实例内权限治理、上传和 artifacts 迁移后，清理旧本地 channel 逻辑；删除后对话、历史、删除、会话列表均走用户实例 websocket 和 `/api/upstream/*`。 | 已修复：删除外层 `/ws`、本地 `/sessions`、旧前端协议分支、`ConnectionRegistry`、`TurnAccumulator`、`SessionQueryService`、`WebUIHook` 和旧 runtime compat 注入；验证：新增 upstream-only 回归测试、相关 Python 测试、前端全量测试和前端构建通过。附件/artifacts 迁移和实例内权限 runtime 后续继续跟踪。 |
| FIX-019 | P1 | 已修复 | 多实例 HR 花名册汇总查询仍存在执行绕路：模型会先读取多份技能文档、查找 CLI 路径、运行 `list-companies` 和 `list-employees`，再运行人数分析；虽然结果正确，但延迟高且在只需汇总时暴露过多员工明细。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-query-analysis-router/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_hr_skill_routing_docs.py` | “员工花名册汇总 / 按公司、部门和在职状态汇总 / 负责公司范围员工概况”必须有明确快路径，直接运行 `business analyze headcount`；技能说明明确不要先运行 `list-employees`，也不要运行 `which` 或 `find` 查找 CLI。 | 已修复：新增红绿静态契约测试，补齐 router 快路径、路由表、`hr-db-ops` 操作规则和 capability focus terms；定向测试通过。 |
| FIX-020 | P1 | 已修复 | 多实例产品形态仍要求用 `nanobot gateway` 启动外层服务，导致外层默认 agent 实例被附带创建；这和“Nanobot 作为 SDK，WebUI 插件作为产品控制面”的架构定位不一致。 | `pyproject.toml`、`src/nanobot_channel_webui/cli.py`、`scripts/publish-local.sh`、`README.md`、`tests/test_webui_cli.py` | 提供 `nanobot-webui gateway` 自有启动命令；该命令只启动 WebUI 控制面，不创建外层默认 AgentLoop；发布提示和文档改为推荐新命令。 | 已修复：新增 `nanobot-webui gateway`、控制面 `/health`、发布脚本安装本插件 tool；验证：`./scripts/publish-local.sh` 通过，已用新命令重启 tmux，健康检查返回 `control_plane`。 |
| FIX-021 | P1 | 已修复 | HR 业务实现脚本仍物理放在 `skills/hr-db-ops/scripts/` 下，容易让模型和后续维护者误以为技能包就是业务执行层。 | `src/nanobot_channel_webui/business_modules/hr/runtime/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`src/nanobot_channel_webui/business_modules/hr/cli.py`、`tests/test_business_cli.py` | 技能包只包含模型可见说明、references 和 `tenant-runtime.json`；真实 HR CLI、policy、repository 和维护脚本迁到插件 HR runtime；`nanobot-webui-business hr ...` 仍正常执行。 | 已修复：HR CLI、policy、repository 和维护脚本迁到 `business_modules/hr/runtime/`；技能目录只剩说明和契约。验证：Python 122 passed、前端 78 passed、发布通过，实例 workspace 下 `business analyze headcount` 返回授权汇总。 |
| FIX-022 | P1 | 已修复 | HR 业务 runtime 仍使用 Node/JS 技术栈，插件需要额外打包 `node_modules`，和 Python 插件主体不一致。 | `src/nanobot_channel_webui/business_modules/hr/runtime/`、`src/nanobot_channel_webui/business_modules/hr/cli.py`、`pyproject.toml`、`scripts/publish-local.sh` | HR CLI、权限策略、repository 和 Supabase connector 全部迁到 Python；发布不再安装或打包 HR `node_modules`；`nanobot-webui-business hr business ...` 仍能通过权限和业务探针。 | 已修复：HR CLI 入口、policy、repository 和 PostgREST/Supabase 访问层已迁到 Python；删除 HR `.mjs`、`package*.json` 和 `node_modules` 打包；验证：`./scripts/publish-local.sh` 通过，wheel 无 HR Node artifacts，普通实例 `business analyze headcount` 返回 `total=25 active=21 resigned=4`。 |
| FIX-023 | P1 | 已修复 | 切换会话时 assistant-ui 保留上一会话的 message/part runtime client，短会话渲染旧索引会触发 `tapClientLookup: Index ... out of bounds` 并导致对话区崩溃；首次修复把 key 放在 `ChatWorkspace` 后又导致 WebSocket 生命周期被反复重建。 | `frontend/src/app.tsx`、`frontend/src/assistant-ui-runtime.ts`、`frontend/src/components/chat/chat-workspace.tsx`、`frontend/src/assistant-ui-runtime.test.ts`、`frontend/src/app-remount-boundary.test.ts` | 多次切换长短不同的会话不会触发 `tapClientLookup` 控制台错误；assistant-ui runtime 在会话切换时清理旧 message/part client；WebSocket-owning `ChatWorkspace` 不随会话切换 remount。 | 已修复：外层 `ChatWorkspace` 保持稳定，只按 `currentChatId` remount 内层 assistant-ui runtime workspace，重新创建 `useExternalStoreRuntime` 的 runtime core，同时保留内容区 key 作为局部防护；验证：前端定向测试通过、`./scripts/publish-local.sh` 通过、已重启 `nanobot-webui gateway`，浏览器连续 10 次切换无新增 `tapClientLookup` 错误。 |
| FIX-024 | P1 | 已修复 | upstream websocket 主链路下仍混用外层账号 session index 概念和用户实例 session，导致会话列表来源、workspace 访问判断、runtime 会话计数和 assistant-ui thread 切换语义不清晰。 | `src/nanobot_channel_webui/channel.py`、`frontend/src/use-websocket-session.ts`、`frontend/src/thread-switch.ts`、`tests/test_channel_managed_instances.py`、`frontend/src/thread-switch-source.test.ts`、`docs/2026-06-05-managed-instance-websocket-channel-mechanism.md` | 会话列表、历史和删除均以用户实例 websocket sidecar 为准；外层账号数据库不作为当前会话列表主存储；切换会话先加载目标历史并防止旧响应覆盖新 thread，再 attach websocket channel。 | 已修复：`_can_access_session` 和 runtime session count 改为读取用户实例 `/api/sessions`；前端新增 thread switch guard，切换流程改为 history-first + latest-only + attach；文档明确 Supabase 只负责登录和权限画像。 |
| FIX-025 | P1 | 已修复 | 托管实例通过插件内 SDK 轻量 runtime 启动，未完整复用上游 `nanobot gateway` 启动语义，导致 Dream、cron、workspace templates、heartbeat、message delivery、session flush 等能力存在漂移风险。 | `src/nanobot_channel_webui/instances/manager.py`、`src/nanobot_channel_webui/instances/builder.py`、`src/nanobot_channel_webui/channel.py`、`tests/test_instance_manager.py` | 每个托管实例由控制面启动 `nanobot gateway --config <instance-config.json>` 子进程；实例 config 和 workspace 写入插件专属 runtime root；控制面只做进程管理和 upstream websocket 代理，不复刻上游 gateway 内部启动流程。 | 已修复并发布：实例改为 CLI 子进程启动，默认目录为 `~/.nanobot-channel-webui/instances`，子进程环境注入 `NANOBOT_CONFIG` 和 `NANOBOT_WEBUI_POLICY_FILE`。验证：`./scripts/publish-local.sh` 通过；浏览器真实 HR 查询成功；实例日志显示模板、Dream、heartbeat、cron 均由上游 gateway 启动。 |
| FIX-026 | P0 | 已修复 | 托管实例继承基础配置中的 `tools.restrictToWorkspace=false`，导致 Nanobot 文件工具和 exec guard 的工作区限制没有启用。 | `src/nanobot_channel_webui/instances/builder.py`、`tests/test_instance_builder.py`、实例 `config.json` | 托管实例无论基础配置如何，都必须写入 `tools.restrictToWorkspace=true`；基础配置保持不变；发布后新启动实例配置生效。 | 已修复：实例 builder 强制设置 `restrict_to_workspace=True`，补回归测试。验证：相关实例测试 18 passed；`./scripts/publish-local.sh` 通过；已重启控制面；两个用户实例落盘 config 均为 `tools.restrictToWorkspace=true`；浏览器刷新后状态为已连接。 |
| FIX-027 | P0 | 已修复 | 启用 `tools.restrictToWorkspace` 后，实例 `workspace/skills/*` 仍是指向插件 site-packages 的符号链接，文件工具解析真实路径后拒绝读取技能说明，模型会尝试 `ls/cat/find/pip` 等绕路。 | `src/nanobot_channel_webui/instances/workspace.py`、`tests/test_instance_workspace.py`、实例 `workspace/skills/` | 授权技能包同步为实例 workspace 内的真实目录；旧 symlink 会在 refresh 时替换成本地副本；`SKILL.md` 真实路径不越过实例 workspace。 | 已修复：实例技能同步从 symlink 改为目录复制，refresh 会替换旧 symlink。验证：实例 workspace 测试红绿通过；相关实例测试 21 passed；`./scripts/publish-local.sh` 通过；已重启控制面并刷新 WebUI；普通用户实例 4 个 HR 技能均为非 symlink，`SKILL.md` resolved path 均在实例 workspace 内。 |
| FIX-028 | P1 | 已修复 | HR 技能 frontmatter description 过度重叠，普通只读查询会同时命中 `hr-query-analysis-router`、`hr-db-ops`、`hr-policy` 和 `hr-schema`，导致模型一次性读取多个 `SKILL.md`。 | `src/nanobot_channel_webui/business_modules/hr/skills/*/SKILL.md`、`tests/test_hr_skill_routing_docs.py` | 普通 HR 只读查询在技能摘要层只明显命中 `hr-query-analysis-router`；`hr-db-ops` 只描述写入和维护入口；`hr-policy` 只描述写入/删除/导入/歧义确认；`hr-schema` 只描述字段映射、导入规划和约束解释。 | 已修复：收窄 4 个 HR 技能 frontmatter description，并补静态契约测试。验证：`tests/test_hr_skill_routing_docs.py` 5 passed；`./scripts/publish-local.sh` 通过；已重启控制面并刷新 WebUI；普通用户实例 workspace 内 4 个技能副本已同步新 description。 |
| FIX-029 | P1 | 已修复 | 设置页技能、配置和运行状态仍读取 WebUI 控制面 workspace，admin 账号看到 `/Users/brian/.nanobot/workspace` 或 `/Users/brian/.nanobot/workspace/skills`，而不是当前登录用户的托管实例 workspace。 | `src/nanobot_channel_webui/channel.py`、`src/nanobot_channel_webui/management.py`、`tests/test_channel_managed_instances.py` | 托管多实例模式下，设置页 Skills、Config、Runtime 和 Contracts 快照按当前登录用户解析到 `~/.nanobot-channel-webui/instances/<user>/workspace`；非托管模式保留控制面 workspace；runtime 文件列表读取实例 sessions/logs/cron。 | 已修复：settings skills/config/runtime/contracts endpoint 在托管模式下按当前用户构造实例管理视图，runtime 日志/cron 路径改为实例结构。验证：新增回归测试通过；`./scripts/publish-local.sh` 通过；已重启控制面；本地计算 admin 实例路径为 `~/.nanobot-channel-webui/instances/user-cm7omdw58z4wv3r/workspace` 且配置存在。 |
| FIX-030 | P1 | 已修复 | 契约校验仍按旧脚本时代假设，把 `tenant-runtime.json` 中的 CLI 命令前缀 `nanobot-webui-business hr` 当成技能目录下的文件路径，导致设置页误报 `command_not_found`。 | `src/nanobot_channel_webui/tenant_runtime/contract_validator.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-query-analysis-router/tenant-runtime.json`、`tests/test_permissions.py` | 契约 `commands` 支持脚本路径和 PATH 上的 CLI 命令前缀；`nanobot-webui-business hr` 在可执行入口存在时不再误报，缺失时仍报错；只读 router 也声明高风险拒绝子命令。 | 已修复：补红灯回归测试并调整校验器命令入口识别；源码 HR 技能契约校验结果为 4 ok、0 warning、0 error。 |
| FIX-031 | P1 | 已修复 | 设置页技能列表和详情页描述显示为 `>`，且 Markdown 预览正文重复渲染 frontmatter 头部，原因是技能文件使用 folded scalar 而管理服务只做简单按行解析。 | `src/nanobot_channel_webui/management.py`、`frontend/src/components/settings/tabs/SkillsTab.tsx`、`frontend/src/types.ts`、`src/nanobot_channel_webui/business_modules/hr/skills/*/SKILL.md`、`tests/test_management.py` | 技能 frontmatter 使用标准 `name: "..."` 和 `description: "..."`；设置页列表和详情显示真实描述；Markdown 正文预览不再显示 `--- name/description ---` 头部；旧 folded scalar 仍能兼容解析。 | 已修复：补管理服务回归测试，接口新增 `preview_content`，前端 Markdown 预览使用剥离 frontmatter 的正文；HR 技能头部已标准化。验证：`tests/test_management.py`、技能文档测试和前端全量测试通过。 |
| FIX-032 | P1 | 进行中 | 移动端对话页响应式状态不稳定：侧边导航展开后可能无法收起，右侧文件/工作区面板会遮断回到对话区的路径，会话历史入口不明显，输入框高度和滚动条影响输入体验。 | `frontend/src/app.tsx`、`frontend/src/components/chat/chat-workspace.tsx`、`frontend/src/components/chat/sidebar.tsx`、`frontend/src/components/chat/conversation-content-pane.tsx`、`frontend/src/components/chat/thread-shell.tsx`、`frontend/src/styles.css` | 窄屏下会话历史可见；侧边导航可展开和收起；右侧面板关闭后可回到对话；输入框高度受控且不显示多余原生滚动条；简单样式修复不默认浏览器冒烟。 | 已补前端响应式 wiring、sidebar close、composer scrollbar 和 CSS 源码级回归覆盖；待完成本轮文档同步后，以下次本地发布或用户指定移动端验收结果更新为 `已修复`。 |

## 维护规则

- 新发现 bug、评审意见或安全风险时，先新增或更新本文件。
- 处理前将状态改为 `进行中`。
- 修复完成后改为 `已修复`，并在备注中记录验证方式、发布时间或关联提交。
- 如果需求发生变化，应标记 `延后` 或 `取消`，不要直接删除历史问题。
