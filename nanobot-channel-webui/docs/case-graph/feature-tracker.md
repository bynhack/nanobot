# Case Graph Feature Tracker

目标：用一张长期维护的功能台账记录当前上图产品已经完成什么、部分完成什么、还缺什么。本文只记录当前 `nanobot-channel-webui` 的产品状态，不再按原版实现做差异跟踪。

## 状态说明

- `Done`: 当前产品路径可用，并已有基本验证或已进入本地发布流程。
- `Partial`: 主链路存在，但仍缺产品闭环、入口、边界能力或更完整验证。
- `Missing`: 目前没有可用产品入口或后端链路。
- `Deferred`: 明确暂不按某种候选方案实现，或已选择替代实现。

## 功能台账

| 功能域 | 能力 | 状态 | 当前实现 | 仍需补齐 |
| --- | --- | --- | --- | --- |
| 图记录生命周期 | 图列表、图 tab、新建图、加载已有图、删除图 | Partial | 已有 saved graph 列表、tab 状态、创建和加载链路；删除图接口会同步删除该图绑定的研判对话，前端只做本地会话状态清理 | 新建图还不会带入当前主体基线；无 tab 时的分析引导仍可优化 |
| 主查询 | 根据侦办起点、金额、时间和钻取配置生成第一张资金关系图 | Partial | 当前产品入口为 `/api/case-graph/relation/query`，前端“分析上图”会调用关系图一跳查询；一跳查询、图内关系查询、全局线索候选和交易事实回填统一基于 `trade_info` 事实视图读取交易流水，默认排除无效流水，并用账户/嫌疑人信息补齐交易主体姓名；关系分析筛选已支持金额和时间范围；查询沉淀的交易事实写入 `facts/trades.jsonl`，包含摘要/备注、流水号、借贷标志、渠道、机构、IP、MAC、设备、商户和订单等后续分析字段；交易核查和交易线详情排除交易后会基于当前图交易事实重算资金线；`excludedTrades` 是当前图交易排除状态，关系图路径基于已沉淀交易事实重算，旧主查询路径仍会沿用它作为兼容输入 | `phone` 固定为空；旧 `/api/case-graph/query` 兼容路径不作为当前产品主入口 |
| 查询运行态 | `sourceSelectId`、`groupMap`、排除状态、筛选状态 | Partial | 已维护 `queryBaselineTradeCards / groupMap / sourceSelectId / excludedTrades / excludedAccountId / minAmount / maxAmount / startTime / endTime / appliedFilters`；线索扩展已改为数据库候选发现和选择补充上图，不再走旧 `summarySelected*` 路径；`summarySelectedAccountId / summarySelectedAccountName / excludedAccountName` 仍属于旧主查询兼容字段，不作为当前线索扩展或节点取消上图的产品状态来源 | 后续清理旧主查询 contract 时，再统一移除或迁移这些兼容字段 |
| 画布渲染 | G6 HTML 节点、资金边、角色视觉、minimap | Done | 使用 G6 图实例、HTML 节点、资金边标签、minimap 和画布工具栏 | 后续如引入更多关系类型，需要扩展节点/边视觉约定 |
| 资金流向图视图 | 将当前资金关系图切换为净流向展示 | Done | 画布工具栏提供“流向”入口；前端基于当前 `graphData / groupMap` 派生只读流向图，双向往来按金额折抵后展示主导方向；弹框内使用从左到右的有向层级布局，不重新查库、不写回布局 | 后续可增强持平往来提示和净流向明细解释 |
| 布局持久化 | 保持已有节点坐标稳定 | Done | `graph.layout.nodePositions` 是权威位置来源，加载、扩图、回放时保留已有坐标 | 只允许显式用户拖拽或未来手动重布局改变已有节点位置 |
| 关系分析 | 一跳分析、主体间关系、补全关系 | Done | 关系操作写入 `graph.json` 当前投影，并追加 `steps/*.json` 步骤快照；关系边保存 `tradeIds`，查询步骤同步沉淀交易事实到 `facts/trades.jsonl`；服务加载图时会把事实文件补回运行时 `tradeFacts`，但磁盘上的 `graph.json` 和步骤文件不再重复写完整事实 | 摘要解释和更多操作类型可继续增强 |
| 研判副驾 | 图谱操作后联动右侧对话解释本次变化 | Done | 图上下文写入当前关系图 `graph.json`、`steps` 目录、`facts/trades.jsonl`、最新步骤和焦点；前端在图谱操作完成后自动选择“图谱研判助手”，让右侧对话解释“本次变化 / 关键线索 / 下一步建议” | 后续可继续增强摘要质量和建议动作的命中率 |
| 对话操作图谱 | 通过右侧对话生成可确认执行的图谱动作 | Partial | 已拆分为“图谱操作助手”负责自然语言操作转译、“图谱研判助手”负责事实解释；图谱对话里明确的改图请求会自动选择图谱操作助手，技能输出隐藏动作注释，前端解析后展示“图谱操作”卡片，用户确认后复用现有筛选、上下钻、取消上图、恢复、补全关系链路执行；对已存在会话里旧技能上下文生成的“请确认是否执行”中文筛选文案，前端有窄范围兜底解析 | 仍需扩展交易核查、批量动作、多轮歧义澄清和更细的执行前确认策略 |
| 全图筛选 | 对当前图按金额、时间筛选资金流水 | Done | 顶部筛选栏调用 `/api/case-graph/relation/filter`，后端基于当前图的 `edge.tradeIds` 与事实文件补回的运行时 `tradeFacts` 重算边金额、笔数和节点，不再重新查库 | 需要交易事实完整；旧的缺事实步骤不作为兼容目标 |
| 钻取 | 双向钻取、上钻、下钻 | Done | 节点右键可触发钻取，钻取后合并图数据并保持已有坐标 | 钻取配置的产品语义仍可继续打磨 |
| 节点取消上图 | 单节点和多节点取消、已排除列表、恢复 | Done | 右键支持取消上图；已排除弹窗支持查看、恢复和恢复全部；操作写入步骤快照；当前产品状态以 `excludedNodes` 为准，不复用旧主查询的名称级排除字段 | 后续如清理旧主查询兼容字段，需要保持节点取消上图链路不受影响 |
| 图交互高亮 | hover / click 一级邻居高亮 | Done | 使用 G6 官方 `hover-activate` 和 `click-select`，并同步 HTML 节点 class | 后续新增交互时要继续避免自定义状态残留 |
| 边交互 | 点击边线打开详情并设置上下文 | Done | 边点击会打开详情，设置会话上下文；详情只展示当前图上该资金线仍保留的交易流水，支持条件筛选、表头排序、滚动浏览、勾选排除和当前结果全选后应用到图；弹窗只展示本线本次勾选状态，已生效的全图排除由图页面全局状态承载；暂不做前端假分页 | 后续可统一补齐后端分页、交易详情证据导出和引用 |
| 交易核查 | 节点关联交易筛选、交易事实沉淀 | Done | 节点右键可打开交易核查；打开后按当前节点全部关联边加载仍在图上的交易流水，并在一个表格中通过“方向、关联主体”列说明来源；支持当前筛选结果多选、全选、清空和表头排序；弹窗只展示本次核查勾选状态，提交时保留已生效的全图排除；勾选排除交易后通过 `/api/case-graph/relation/exclude-trades` 基于事实文件补回的运行时 `tradeFacts` 和 `edgeTradeIds` 本地重算边金额/次数，并追加“交易核查”步骤 | 后续可继续增强批量加载策略、证据导出和恢复已完全移除边的交易 |
| 步骤回放 | 上一步、下一步、当前状态、自定义回放面板 | Done | 从 `steps/*.json` 构建时间线；历史预览只读；最新步骤自动回到当前态；步骤切换复用 G6 动画 | 任意历史步骤恢复为当前图、完整 reset 到指定步骤仍未实现 |
| 步骤动画 | 新增节点/边 reveal 动画、快速切换清理 | Done | 只在有连续图变化时动画；切步骤前后清理 transient state；过期 reveal 异步不会写回 DOM | 后续如增加更多动画状态，需要纳入同一 transient 清理机制 |
| 官方 timebar | 使用 G6 timebar 插件 | Deferred | 已选择自定义回放面板，避免官方 timebar 与业务步骤状态重复 | 除非要采用官方插件能力，否则不需要恢复 |
| 群组 | 自动同名分组、群组节点和群组边 | Partial | 后端可参与查询和聚边，前端可展示群组节点并支持详情解析 | 缺取消群组、重组、解除分组等当前产品交互 |
| 线索扩展 | 从数据库交易流水中发现候选主体，并和当前图已上图状态比对后选择补充 | Done | 节点右键提供“线索扩展”入口，围绕当前节点读取未套用默认钻取数量、金额或时间条件的上下游候选主体，应用时只把选中候选及其与当前主体之间的资金线补充进图；画布空白右键提供“全局线索扩展”入口，从案件交易流水全局汇总候选主体，应用时先把选中主体加入当前图谱，供后续节点钻取或“关系”功能继续验证连接；两种入口都会标记“可补充 / 已上图 / 已取消上图”，支持按主体、金额、时间、方向、笔数筛选、表头排序和全选，并会恢复选中候选的已取消状态，不把未选候选写成排除项，也不恢复旧 `summarySelected*` 路径 | 后续可扩展线索扩展结果解释、证据导出和对话直接触发 |
| 人工补充线索 | 手动补充图上资金往来、交易主体和现实关系 | Done | 在已有案件图和图上主体基础上，节点右键和非空画布右键提供“创建交易主体 / 补充资金往来 / 标注现实关系”入口；办案人员可在画布空白处创建只存在于当前图谱的交易主体，记录银行卡号或账号、发现原因、来源材料和情况说明；补充资金往来和标注现实关系的两端主体选择支持输入筛选，补充资金往来形成不入数据库的人工交易事实、交易线和步骤快照，人工交易事实同样落到 `facts/trades.jsonl`；标注现实关系会写入 `graph.realityRelations`，画布用独立虚线关系展示，不参与资金金额、笔数、筛选和统计 | 后续可补编辑、删除、附件证据、关系类型治理和对话直接触发 |
| 话单关系 | `phone` 关系查询和渲染 | Missing | 查询结果保留 `phone` 字段 | 缺后端话单查询链路、前端关系类型和画布渲染 |
| 完整画布能力 | 非关系图元、历史步骤恢复、完整 reset/restore | Partial | 当前已保存关系图投影、布局位置和步骤快照，并支持步骤只读预览 | 任意历史步骤恢复为当前图、完整 reset、非关系图元保留仍未实现 |

## 当前优先补齐队列

1. P0: 实现 `phone` 话单关系链路，包含后端查询、前端类型、画布渲染。
2. P1: 扩展“对话直接操作图谱”的动作协议，继续覆盖交易核查、线索扩展、批量动作、多轮歧义澄清和更细的执行前确认策略。
3. P1: 完善群组交互，包括取消群组、重组或解除分组。
4. P1: 支持从任意历史步骤恢复为当前图，或明确产品上只允许历史预览。
5. P1: 增强证据链输出，包括交易引用编号、证据导出和已完全移除资金线的恢复入口。
6. P2: 完善人工补充线索的编辑、删除、附件证据和关系类型治理。

## 维护规则

- 功能状态变化时，优先更新本文。
- 如果状态变化会改变待修复问题或产品决策，同步更新 `pending-fixes.md`。
- 不把临时计划、日期化发布笔记写进本文；这里保存长期有效的能力状态。
- 每个 `Done` 项尽量保留代码证据、测试证据或本地发布证据，避免只凭记忆标记完成。
