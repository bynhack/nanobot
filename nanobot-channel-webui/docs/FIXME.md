# 修复清单

本文件用于跟踪已经确认的问题、风险和技术债。任何代码修复前应先确认本清单是否已有对应条目；没有则新增，修复完成后更新状态、证据和后续项。

## 状态约定

- `待处理`：已确认，需要修复。
- `进行中`：当前正在处理。
- `已修复`：代码已修改并通过必要验证。
- `延后`：暂不处理，需要说明原因。
- `取消`：确认不再需要处理，需要说明原因。

## 优先级约定

- `P0`：安全边界、数据泄露、权限绕过、服务不可用，必须优先处理。
- `P1`：稳定性、重要流程一致性、可维护性问题，近期处理。
- `P2`：整理、重构、文档同步、体验优化，可排期处理。

## 当前修复项

| ID | 优先级 | 状态 | 问题 | 涉及位置 | 验收标准 | 备注 |
|---|---|---|---|---|---|---|
| FIX-001 | P0 | 已修复 | scoped 用户下通用工具层存在默认放行链路，`read_file`、`list_dir`、`exec` 和未知工具的边界需要重新收口，同时保留普通办公技能自由组合能力。 | `src/nanobot_channel_webui/permissions/tool_registry.py`、`src/nanobot_channel_webui/permissions/command_policy.py` | scoped 用户不能读取敏感文件、枚举敏感目录或通过 shell 绕过受控业务 CLI；Word、Excel、浏览器等普通技能仍可基于授权 artifacts 正常使用。 | 已发布验证：权限测试通过；敏感 `read_file/list_dir/exec` 被拒绝，普通 `create_docx` 透传。 |
| FIX-002 | P0 | 已修复 | 敏感路径保护不完整，`~/.nanobot/config.json`、sessions JSONL、audit、private、policies、配置文件等需要纳入读取/列目录边界。 | `src/nanobot_channel_webui/permissions/tool_registry.py` | scoped 用户无法通过 `read_file` 或 `list_dir` 获取敏感配置、会话历史、权限文件、审计文件和私有运行时文件。 | 已发布验证：`~/.nanobot/config.json`、workspace root、sessions/audit/private/policies 均被拒绝。 |
| FIX-003 | P0 | 已修复 | 权限上下文存在 fail-open 默认值：无用户、缺 role、缺 policy file、缺 `NANOBOT_WEBUI_POLICY_FILE` 时可能退化为 admin/unrestricted。 | `src/nanobot_channel_webui/permissions/context.py`、`src/nanobot_channel_webui/permissions/resolver.py`、`src/nanobot_channel_webui/permissions/command_policy.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py` | scoped 或身份缺失场景默认拒绝受控业务访问；只有显式 admin policy 才 unrestricted；已认证 admin 保持 unrestricted。 | 已发布验证：fail-closed 默认值保持不变；`resolve(None)` 非特权；已认证 admin 分支显式构造 `role=admin/business_role=admin`，回归测试 `test_policy_resolver_admin_is_unrestricted` 已覆盖；`./scripts/publish-local.sh` 通过。 |
| FIX-004 | P0 | 已修复 | 身份证号和手机号查询没有强制 company scope，可能导致跨公司 PII 泄露。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 使用身份证号或手机号查询时，无论是否显式传 `--company`，结果都必须限制在当前账号授权公司范围内。 | 已修复：证件号和手机号查询统一下推 `companyName/companyNames`，显式公司也会校验 scope。 |
| FIX-005 | P1 | 已修复 | 权限运行时注入失败时可能静默降级，scoped 请求仍继续执行。 | `src/nanobot_channel_webui/permissions/injector.py`、`src/nanobot_channel_webui/channel.py` | scoped 请求在权限 runtime 未 attached 时硬失败；管理端观测信息能明确显示注入状态。 | 已修复：tenant runtime 注入失败时 runtime 标记失败，scoped message/cancel 请求直接拒绝。 |
| FIX-006 | P1 | 已修复 | 写入 scope 只在 CLI 入口扫描 JSON plan，repository 最终写入层不感知授权 scope。 | `src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py` | 写入、删除、验证在 repository 层也能基于授权公司范围做最终校验。 | 已修复：repository 写入、删除、验证入口统一调用 plan 公司范围兜底校验；CLI 入口仍保留第一道权限校验。 |
| FIX-007 | P1 | 已修复 | scoped 用户的工具定义仍可能暴露完整工具表，和实际权限边界不一致。 | `src/nanobot_channel_webui/permissions/tool_registry.py` | 模型看到的工具定义和 scoped 用户可执行能力一致；普通办公工具保留，敏感工具或敏感参数面不暴露。 | 已修复：scoped 工具定义隐藏 `grep/edit_file/spawn/my/cron/long_task/notebook_edit` 等敏感运行时工具；普通办公类工具继续透传。 |
| FIX-008 | P1 | 已修复 | `business delete` 流程没有和 create/update 一样统一补内部确认语义，自然语言删除流程不稳定。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 删除类 business 命令走统一 preview/确认/执行/验证流程，用户不需要理解底层 `--confirm`。 | 已修复：`business delete employee` 自动补内部确认语义。 |
| FIX-009 | P2 | 已修复 | HR 任务聚焦词硬编码在通用权限层，影响多业务域通用性。 | `src/nanobot_channel_webui/permissions/skill_view.py`、各业务 `tenant-runtime.json` | 业务术语、focus terms 和 related capabilities 由业务契约声明，通用运行时只读取契约。 | 已修复：新增 capability `focus_terms` 契约字段，HR 词表迁入 HR `tenant-runtime.json`，通用动态视图渲染器不再硬编码 HR 词表。 |
| FIX-010 | P2 | 已修复 | `permissions` 与 `tenant_runtime` 双模块命名和再导出较多，长期维护成本高。 | `src/nanobot_channel_webui/permissions/`、`src/nanobot_channel_webui/tenant_runtime/` | 明确主模块边界，减少重复导出和兼容别名。 | 已修复：明确 `tenant_runtime` 为新公共命名层，`permissions` 为当前实现和兼容导入层；不做破坏性删除，避免影响已有 import。 |
| FIX-011 | P2 | 已修复 | `TenantGuard` 当前是旁路 SDK，有测试和导出，但未进入 HR 主执行链路，文档表述容易误导。 | `src/nanobot_channel_webui/tenant_runtime/guard.py`、`docs/` | 文档准确描述当前 enforcement 主链路；若保留 SDK，要说明适用场景。 | 已修复：权限硬化文档已明确 HR 主链路由命令网关、动态技能视图、HR CLI 和 repository scope 兜底共同完成；`TenantGuard` 是新业务技能可复用 SDK。 |
| FIX-012 | P2 | 已修复 | 存在重复 JS guard 文件和 `__pycache__`、旧 `case_graph` 缓存残留。 | `src/nanobot_channel_webui/tenant_runtime/*.mjs`、`src/**/__pycache__`、`src/nanobot_channel_webui/case_graph/` | 重复文件和缓存残留清理完成，打包产物不包含无关缓存。 | 已修复：删除重复 `tenant_runtime_guard.mjs`，清理缓存，并在发布验证脚本编译后自动删除 `__pycache__`。 |
| FIX-013 | P2 | 已修复 | help 输出仍混有 legacy 命令和 business 命令，容易让模型执行绕路。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py` | 面向智能体的 help 只暴露标准 business surface；legacy 命令仅作为内部兼容 alias。 | 已修复：默认 `help` 输出标准 business surface；legacy 命令保留为兼容 alias 和特定命令帮助。 |
| FIX-014 | P0 | 已修复 | HR CLI 直调 `count-all` 必须走业务 runtime 授权，不能只依赖 Python exec 网关的 `denied_commands` 兜底。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`tests/test_permissions.py` | scoped 用户直调 `nanobot-webui-business hr count-all` 时被 Python policy 拒绝；拒绝发生在数据库访问前；admin/unrestricted 仍可执行全局统计。 | 已修复并补回归测试：`count-all` case 调用 `authorize_hr_command`，命中 `SCOPED_GLOBAL_DENY_COMMANDS`；`test_hr_cli_count_all_is_denied_by_python_policy_for_scoped_user` 覆盖 CLI 直调。 |
| FIX-015 | P1 | 已修复 | assistant-ui runtime 在同一条消息内收到重复 `toolCallId` 时触发 `Duplicate key ... in tapResources`，导致页面崩溃。 | `frontend/src/app-helpers.ts`、`frontend/src/app-helpers.test.ts` | 重复上游 `callId` 不会生成重复 assistant-ui `toolCallId`；页面保持可渲染，工具调用仍可展示。 | 已修复：assistant-ui 投影层对同一消息内重复 `toolCallId` 稳定消歧；验证：新增回归测试失败后修复，前端定向测试通过，`./scripts/publish-local.sh` 通过并已重启 gateway。 |
| FIX-016 | P1 | 已修复 | 托管实例模式删除会话时 `/api/upstream/sessions/{chat_id}` 返回 502，浏览器显示“删除会话失败”。 | `src/nanobot_channel_webui/channel.py`、`tests/test_channel_managed_instances.py` | 浏览器仍可向 WebUI 插件发 `DELETE`；插件代理到 Nanobot websocket sidecar 时使用其支持的 `GET /api/sessions/{key}/delete`；删除请求不触发 websocket HTTP parser 错误。 | 已修复：根因是 Nanobot websocket sidecar 基于 `websockets`，只接受 HTTP `GET`；内部代理恢复 GET 并补回归测试。验证：`tests/test_channel_managed_instances.py` 和全量 Python 测试通过。 |
| FIX-017 | P1 | 已修复 | 多实例 HR 查询存在执行绕路：实例 CLI 没有自动带入 policy 文件，模型首次调用失败后会自行查找敏感运行时路径；公司和部门查询被拆成 N+1 次 CLI。 | `src/nanobot_channel_webui/business_modules/hr/cli.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_business_cli.py`、`tests/test_permissions.py` | 实例 workspace 下运行 `nanobot-webui-business hr ...` 自动发现 `.nanobot_channel_webui/policies/policy.json`；“可见公司及部门”可通过单条 `business query organization-tree` 完成；权限网关仍识别该命令并注入 policy env。 | 已修复：新增 policy 自动注入、`organization-tree` 聚合命令、HR 契约 capability 和短路由说明；验证：新增红绿回归测试和权限网关定向测试通过。 |
| FIX-018 | P1 | 已修复 | 旧 WebUI channel 本地对话逻辑仍保留在主代码中：`/ws`、外层 session 存储、前端 fallback 协议、外层 AgentLoop hook 注入等与多实例 upstream 主链路重叠。 | `src/nanobot_channel_webui/channel.py`、`frontend/src/ws-client.ts`、`frontend/src/use-websocket-session.ts`、`frontend/src/api.ts`、`src/nanobot_channel_webui/permissions/` | 在确认 upstream-only 产品边界、实例内权限治理、上传和 artifacts 迁移后，清理旧本地 channel 逻辑；删除后对话、历史、删除、会话列表均走用户实例 websocket 和 `/api/upstream/*`。 | 已修复：删除外层 `/ws`、本地 `/sessions`、旧前端协议分支、`ConnectionRegistry`、`TurnAccumulator`、`SessionQueryService`、`WebUIHook` 和旧 runtime compat 注入；验证：新增 upstream-only 回归测试、相关 Python 测试、前端全量测试和前端构建通过。附件/artifacts 迁移和实例内权限 runtime 后续继续跟踪。 |
| FIX-019 | P1 | 已修复 | 多实例 HR 花名册汇总查询仍存在执行绕路：模型会先读取多份技能文档、查找 CLI 路径、运行 `list-companies` 和 `list-employees`，再运行人数分析；虽然结果正确，但延迟高且在只需汇总时暴露过多员工明细。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-query-analysis-router/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_hr_skill_routing_docs.py` | “员工花名册汇总 / 按公司、部门和在职状态汇总 / 负责公司范围员工概况”必须有明确快路径，直接运行 `business analyze headcount`；技能说明明确不要先运行 `list-employees`，也不要运行 `which` 或 `find` 查找 CLI。 | 已修复：新增红绿静态契约测试，补齐 router 快路径、路由表、`hr-db-ops` 操作规则和 capability focus terms；定向测试通过。 |
| FIX-020 | P1 | 已修复 | 多实例产品形态仍要求用 `nanobot gateway` 启动外层服务，导致外层默认 agent 实例被附带创建；这和“Nanobot 作为 SDK，WebUI 插件作为产品控制面”的架构定位不一致。 | `pyproject.toml`、`src/nanobot_channel_webui/cli.py`、`scripts/publish-local.sh`、`README.md`、`tests/test_webui_cli.py` | 提供 `nanobot-webui gateway` 自有启动命令；该命令只启动 WebUI 控制面，不创建外层默认 AgentLoop；发布提示和文档改为推荐新命令。 | 已修复：新增 `nanobot-webui gateway` 和控制面 `/health`；发布脚本现通过 `uv tool install nanobot-ai --with <wheel> --force` 将插件安装进单一 Nanobot tool env；新版 uv 使用 `--with-executables-from` 暴露 WebUI 命令，旧版 uv 自动创建指向 `nanobot-ai` tool env 的命令 shim。验证：`./scripts/publish-local.sh` 通过，已用新命令重启 tmux，健康检查返回 `control_plane`。 |
| FIX-021 | P1 | 已修复 | HR 业务实现脚本仍物理放在 `skills/hr-db-ops/scripts/` 下，容易让模型和后续维护者误以为技能包就是业务执行层。 | `src/nanobot_channel_webui/business_modules/hr/runtime/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`src/nanobot_channel_webui/business_modules/hr/cli.py`、`tests/test_business_cli.py` | 技能包只包含模型可见说明、references 和 `tenant-runtime.json`；真实 HR CLI、policy、repository 和维护脚本迁到插件 HR runtime；`nanobot-webui-business hr ...` 仍正常执行。 | 已修复：HR CLI、policy、repository 和维护脚本迁到 `business_modules/hr/runtime/`；技能目录只剩说明和契约。验证：Python 122 passed、前端 78 passed、发布通过，实例 workspace 下 `business analyze headcount` 返回授权汇总。 |
| FIX-022 | P1 | 已修复 | HR 业务 runtime 仍使用 Node/JS 技术栈，插件需要额外打包 `node_modules`，和 Python 插件主体不一致。 | `src/nanobot_channel_webui/business_modules/hr/runtime/`、`src/nanobot_channel_webui/business_modules/hr/cli.py`、`pyproject.toml`、`scripts/publish-local.sh` | HR CLI、权限策略、repository 和 Supabase connector 全部迁到 Python；发布不再安装或打包 HR `node_modules`；`nanobot-webui-business hr business ...` 仍能通过权限和业务探针。 | 已修复：HR CLI 入口、policy、repository 和 PostgREST/Supabase 访问层已迁到 Python；删除 HR `.mjs`、`package*.json` 和 `node_modules` 打包；验证：`./scripts/publish-local.sh` 通过，wheel 无 HR Node artifacts，普通实例 `business analyze headcount` 返回 `total=25 active=21 resigned=4`。 |
| FIX-023 | P1 | 已修复 | 切换会话时 assistant-ui 保留上一会话的 message/part runtime client，短会话渲染旧索引会触发 `tapClientLookup: Index ... out of bounds` 并导致对话区崩溃；首次修复把 key 放在 `ChatWorkspace` 后又导致 WebSocket 生命周期被反复重建。 | `frontend/src/app.tsx`、`frontend/src/assistant-ui-runtime.ts`、`frontend/src/components/chat/chat-workspace.tsx`、`frontend/src/assistant-ui-runtime.test.ts`、`frontend/src/app-remount-boundary.test.ts` | 多次切换长短不同的会话不会触发 `tapClientLookup` 控制台错误；assistant-ui runtime 在会话切换时清理旧 message/part client；WebSocket-owning `ChatWorkspace` 不随会话切换 remount。 | 已修复：外层 `ChatWorkspace` 保持稳定，只按 `currentChatId` remount 内层 assistant-ui runtime workspace，重新创建 `useExternalStoreRuntime` 的 runtime core，同时保留内容区 key 作为局部防护；验证：前端定向测试通过、`./scripts/publish-local.sh` 通过、已重启 `nanobot-webui gateway`，浏览器连续 10 次切换无新增 `tapClientLookup` 错误。 |
| FIX-024 | P1 | 已修复 | upstream websocket 主链路下仍混用外层账号 session index 概念和用户实例 session，导致会话列表来源、workspace 访问判断、runtime 会话计数和 assistant-ui thread 切换语义不清晰。 | `src/nanobot_channel_webui/channel.py`、`frontend/src/use-websocket-session.ts`、`frontend/src/thread-switch.ts`、`tests/test_channel_managed_instances.py`、`frontend/src/thread-switch-source.test.ts`、`docs/2026-06-05-managed-instance-websocket-channel-mechanism.md` | 会话列表、历史和删除均以用户实例 websocket sidecar 为准；外层账号数据库不作为当前会话列表主存储；切换会话先加载目标历史并防止旧响应覆盖新 thread，再 attach websocket channel。 | 已修复：`_can_access_session` 和 runtime session count 改为读取用户实例 `/api/sessions`；前端新增 thread switch guard，切换流程改为 history-first + latest-only + attach；文档明确 Supabase 只负责登录和权限画像。 |
| FIX-025 | P1 | 已修复 | 托管实例通过插件内 SDK 轻量 runtime 启动，未完整复用上游 `nanobot gateway` 启动语义，导致 Dream、cron、workspace templates、heartbeat、message delivery、session flush 等能力存在漂移风险。 | `src/nanobot_channel_webui/instances/manager.py`、`src/nanobot_channel_webui/instances/builder.py`、`src/nanobot_channel_webui/channel.py`、`tests/test_instance_manager.py` | 每个托管实例由控制面启动 `nanobot gateway --config <instance-config.json>` 子进程；实例 config 和 workspace 写入插件专属 runtime root；控制面只做进程管理和 upstream websocket 代理，不复刻上游 gateway 内部启动流程。 | 已修复并发布：实例改为 CLI 子进程启动，默认目录为 `~/.nanobot-channel-webui/instances`，子进程环境注入 `NANOBOT_CONFIG` 和 `NANOBOT_WEBUI_POLICY_FILE`。验证：`./scripts/publish-local.sh` 通过；浏览器真实 HR 查询成功；实例日志显示模板、Dream、heartbeat、cron 均由上游 gateway 启动。 |
| FIX-026 | P0 | 已修复 | 托管实例继承基础配置中的 `tools.restrictToWorkspace=false`，导致 Nanobot 文件工具和 exec guard 的工作区限制没有启用。 | `src/nanobot_channel_webui/instances/builder.py`、`tests/test_instance_builder.py`、实例 `config.json` | 托管实例无论基础配置如何，都必须写入 `tools.restrictToWorkspace=true`；基础配置保持不变；发布后新启动实例配置生效。 | 已修复：实例 builder 强制设置 `restrict_to_workspace=True`，补回归测试。验证：相关实例测试 18 passed；`./scripts/publish-local.sh` 通过；已重启控制面；两个用户实例落盘 config 均为 `tools.restrictToWorkspace=true`；浏览器刷新后状态为已连接。 |
| FIX-027 | P0 | 已修复 | 启用 `tools.restrictToWorkspace` 后，实例 `workspace/skills/*` 仍是指向插件 site-packages 的符号链接，文件工具解析真实路径后拒绝读取技能说明，模型会尝试 `ls/cat/find/pip` 等绕路。 | `src/nanobot_channel_webui/instances/workspace.py`、`tests/test_instance_workspace.py`、实例 `workspace/skills/` | 授权技能包同步为实例 workspace 内的真实目录；旧 symlink 会在 refresh 时替换成本地副本；`SKILL.md` 真实路径不越过实例 workspace。 | 已修复：实例技能同步从 symlink 改为目录复制，refresh 会替换旧 symlink。验证：实例 workspace 测试红绿通过；相关实例测试 21 passed；`./scripts/publish-local.sh` 通过；已重启控制面并刷新 WebUI；普通用户实例 4 个 HR 技能均为非 symlink，`SKILL.md` resolved path 均在实例 workspace 内。 |
| FIX-028 | P1 | 已修复 | HR 技能 frontmatter description 过度重叠，普通只读查询会同时命中 `hr-query-analysis-router`、`hr-db-ops`、`hr-policy` 和 `hr-schema`，导致模型一次性读取多个 `SKILL.md`。 | `src/nanobot_channel_webui/business_modules/hr/skills/*/SKILL.md`、`tests/test_hr_skill_routing_docs.py` | 普通 HR 只读查询在技能摘要层只明显命中 `hr-query-analysis-router`；`hr-db-ops` 只描述写入和维护入口；`hr-policy` 只描述写入/删除/导入/歧义确认；`hr-schema` 只描述字段映射、导入规划和约束解释。 | 已修复：收窄 4 个 HR 技能 frontmatter description，并补静态契约测试。验证：`tests/test_hr_skill_routing_docs.py` 5 passed；`./scripts/publish-local.sh` 通过；已重启控制面并刷新 WebUI；普通用户实例 workspace 内 4 个技能副本已同步新 description。 |
| FIX-029 | P1 | 已修复 | 设置页技能、配置和运行状态仍读取 WebUI 控制面 workspace，admin 账号看到 `/Users/brian/.nanobot/workspace` 或 `/Users/brian/.nanobot/workspace/skills`，而不是当前登录用户的托管实例 workspace。 | `src/nanobot_channel_webui/channel.py`、`src/nanobot_channel_webui/management.py`、`tests/test_channel_managed_instances.py` | 托管多实例模式下，设置页 Skills、Config、Runtime 和 Contracts 快照按当前登录用户解析到 `~/.nanobot-channel-webui/instances/<user>/workspace`；非托管模式保留控制面 workspace；runtime 文件列表读取实例 sessions/logs/cron。 | 已修复：settings skills/config/runtime/contracts endpoint 在托管模式下按当前用户构造实例管理视图，runtime 日志/cron 路径改为实例结构。验证：新增回归测试通过；`./scripts/publish-local.sh` 通过；已重启控制面；本地计算 admin 实例路径为 `~/.nanobot-channel-webui/instances/user-cm7omdw58z4wv3r/workspace` 且配置存在。 |
| FIX-030 | P1 | 已修复 | 契约校验仍按旧脚本时代假设，把 `tenant-runtime.json` 中的 CLI 命令前缀 `nanobot-webui-business hr` 当成技能目录下的文件路径，导致设置页误报 `command_not_found`。 | `src/nanobot_channel_webui/tenant_runtime/contract_validator.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-query-analysis-router/tenant-runtime.json`、`tests/test_permissions.py` | 契约 `commands` 支持脚本路径和 PATH 上的 CLI 命令前缀；`nanobot-webui-business hr` 在可执行入口存在时不再误报，缺失时仍报错；只读 router 也声明高风险拒绝子命令。 | 已修复：补红灯回归测试并调整校验器命令入口识别；源码 HR 技能契约校验结果为 4 ok、0 warning、0 error。 |
| FIX-031 | P1 | 已修复 | 设置页技能列表和详情页描述显示为 `>`，且 Markdown 预览正文重复渲染 frontmatter 头部，原因是技能文件使用 folded scalar 而管理服务只做简单按行解析。 | `src/nanobot_channel_webui/management.py`、`frontend/src/components/settings/tabs/SkillsTab.tsx`、`frontend/src/types.ts`、`src/nanobot_channel_webui/business_modules/hr/skills/*/SKILL.md`、`tests/test_management.py` | 技能 frontmatter 使用标准 `name: "..."` 和 `description: "..."`；设置页列表和详情显示真实描述；Markdown 正文预览不再显示 `--- name/description ---` 头部；旧 folded scalar 仍能兼容解析。 | 已修复：补管理服务回归测试，接口新增 `preview_content`，前端 Markdown 预览使用剥离 frontmatter 的正文；HR 技能头部已标准化。验证：`tests/test_management.py`、技能文档测试和前端全量测试通过。 |
| FIX-032 | P1 | 进行中 | 移动端对话页响应式状态不稳定：侧边导航展开后可能无法收起，右侧文件/工作区面板会遮断回到对话区的路径，会话历史入口不明显，输入框高度和滚动条影响输入体验。 | `frontend/src/app.tsx`、`frontend/src/components/chat/chat-workspace.tsx`、`frontend/src/components/chat/sidebar.tsx`、`frontend/src/components/chat/conversation-content-pane.tsx`、`frontend/src/components/chat/thread-shell.tsx`、`frontend/src/styles.css` | 窄屏下会话历史可见；侧边导航可展开和收起；右侧面板关闭后可回到对话；输入框高度受控且不显示多余原生滚动条；简单样式修复不默认浏览器冒烟。 | 已补前端响应式 wiring、sidebar close、composer scrollbar 和 CSS 源码级回归覆盖；待完成本轮文档同步后，以下次本地发布或用户指定移动端验收结果更新为 `已修复`。 |
| FIX-033 | P1 | 已修复 | HR 写入契约声明和实际 CLI 不一致：支持新增的数据表缺少通用更新命令，`tenant-runtime.json` 声明的 `business update employee-nickname` 也没有 CLI alias，导致模型在用户要求更新时只能反复尝试 create 或不存在的 update。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/tenant-runtime.json`、`tests/test_business_cli.py`、`tests/test_hr_skill_routing_docs.py` | 所有支持 create 的 HR 资源都有 preview-update/update 命令；更新命令走人工确认、权限校验、唯一匹配、字段差异和 verification；help、技能说明和 tenant runtime 契约一致；焦文明身份证有效期这类员工主档字段更新可通过标准 CLI 完成。 | 已修复并发布：新增红绿测试覆盖员工和合同 update、create/update 资源矩阵、公开契约；`tests/test_business_cli.py`、`tests/test_hr_skill_routing_docs.py`、`tests/test_permissions.py` 共 79 passed；最终 `./scripts/publish-local.sh` 通过，包含 Python 142 passed、前端 97 passed、构建和编译；已重启控制面，`curl http://127.0.0.1:8081/health` 返回 `{"status":"ok","mode":"control_plane"}`；真实实例 preview-only 探针显示焦文明 `id_card_expiry` 将从 `1983-09-17` 更新到 `2043-08-11`。 |
| FIX-034 | P1 | 已修复 | `hr-schema` 技能指导文件和当前 Supabase HR 数据库存在字段漂移：员工档案缺少档案延续字段，合同缺少 `company_id`，招聘岗位仍记录线上不存在的 `company_id`，容易误导模型生成错误查询或写入计划。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-schema/references/all-migrations-zh.sql`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-schema/references/database-schema-zh.md`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-schema/references/schema-summary.md` | `hr-schema` 字段清单和当前 PostgREST OpenAPI 暴露的 HR 表字段一致；员工档案延续字段、合同公司归属和奖惩记录 `incident_dates date[]` 表述准确；招聘岗位不再声明不存在的公司外键。 | 已修复并发布：PostgREST OpenAPI 对比 14 张 HR 表字段均为 `missing=[] extra=[]`；`tests/test_hr_skill_routing_docs.py` 5 passed；`./scripts/publish-local.sh` 通过，包含 Python 142 passed、前端 97 passed、构建和编译；已重启控制面，`curl http://127.0.0.1:8081/health` 返回 `{"status":"ok","mode":"control_plane"}`；已确认 `user-9516ae6d-9b71-4c38-ac1c-82ef6a6dc735` 实例 workspace 内 `hr-schema` 副本已同步新字段。 |
| FIX-035 | P1 | 已修复 | upstream websocket 主链路下附件上传仍沿用旧外层逻辑：前端发送消息时丢弃 attachments，后端上传文件落到控制面 workspace，托管实例开启 `tools.restrictToWorkspace=true` 后模型读不到附件。 | `frontend/src/app-helpers.ts`、`frontend/src/use-websocket-session.ts`、`src/nanobot_channel_webui/channel.py`、`tests/test_channel_managed_instances.py`、`frontend/src/app-helpers.test.ts` | 上传成功后，发送到用户实例 upstream websocket 的正文包含附件源路径；托管实例模式下上传文件写入当前登录用户的实例 workspace；非托管模式保留控制面 workspace；无权限会话不能上传。 | 已修复并发布：新增红绿回归测试覆盖附件源路径拼接和托管实例上传目录；`./scripts/publish-local.sh` 通过，包含 Python 143 passed、前端 99 passed、构建和编译；已重启 `nanobot-webui gateway`，`curl http://127.0.0.1:8081/health` 返回 `{"status":"ok","mode":"control_plane"}`。 |
| FIX-036 | P1 | 已修复 | 托管实例通过 upstream websocket `message` 工具交付的文件预览链接指向用户实例 sidecar 的 `/api/media/...`，浏览器在控制面端口访问该相对路径时命中错误服务；同时 assistant-ui 文件卡片使用裸 `<a href>`，不会自动带前端 localStorage 中的登录 token；部分 HTML 附件被标记为 `application/octet-stream` 后没有按 `.html` 后缀兜底；刷新页面从 upstream history 加载时，`type: outbound` / `media` 文件消息没有被转换回前端文件卡片；旧代理仍依赖上游 media 签名，服务重启或签名过期后历史附件无法下载或 iframe 预览空白；右侧工作空间只读本地交付索引，没有把 upstream history 中的 media 纳入交付文件，显示 `0 个交付文件`；历史重建只读取 camelCase `toolEvents`，导致上游 JSONL / history payload 中的 snake_case `tool_events` 工具调用刷新后丢失；上游 `webui-thread` schema v3 把 `write_file` 放在 `fileEdits` 而不是 `toolEvents`，前端历史映射漏掉文件编辑工具调用；上游 `webui-thread` 投影只收集紧贴 `assistant.tool_calls` 后面的连续 `role: tool`，中间夹 `_channel_delivery` / media 消息时后续工具结果丢失；`message` 工具被直接转成 outbound 文件消息后没有保留工具记录；发布安装方式曾分裂为 WebUI 控制面 tool env 和托管实例使用的 `nanobot-ai` tool env，容易导致两边插件版本不同步。 | `src/nanobot_channel_webui/channel.py`、`src/nanobot_channel_webui/history_projection.py`、`scripts/publish-local.sh`、`README.md`、`frontend/src/api.ts`、`frontend/src/app-helpers.ts`、`frontend/src/use-websocket-session.ts`、`frontend/src/components/assistant-ui/file.tsx`、`frontend/src/preview-workspace/media-types.ts`、`tests/test_channel_managed_instances.py`、`tests/test_history_projection.py`、`frontend/src/app-helpers.test.ts`、`frontend/src/components/assistant-ui/file.test.tsx`、`frontend/src/preview-workspace/media-router.test.tsx`、`frontend/src/store.workspace.test.ts` | 实时 websocket `media_urls` 和 upstream thread payload 中的相对 `/api/media/...` 链接重写为稳定控制面 `/api/public-media/{payload}`；控制面直接按 payload 解析实例 `media/` 目录中的文件，不依赖登录 token、上游 sidecar token 或上游签名；旧的 `/api/upstream/media/{sig}/{payload}` 和 `/api/media/{sig}/{payload}` 也直接走同一解析；空 MIME 或通用二进制 MIME 按文件名后缀兜底，`.html` 进入 HTML 预览器；上游 `webui-thread` 投影按 `tool_call_id` 向后匹配结果行，允许中间夹文件交付消息；`message` 工具既保留 outbound 文件消息，也保留工具调用记录；刷新历史时兼容 `toolEvents`、`tool_events`、schema v3 `fileEdits`、`assistant.tool_calls` 和后续 `role: tool` 结果行；带 schema v3 media 对象的 assistant 交付消息会补出 `message` 工具记录；`publish-local.sh` 使用单一 `nanobot-ai` tool env 安装插件 wheel；新版 uv 通过 `--with-executables-from` 暴露 WebUI 命令，旧版 uv 通过指向 `nanobot-ai` tool env 的命令 shim 暴露；工作空间 endpoint 从 upstream thread 汇总 media 并写入交付索引；远程 URL 保持不变。 | 已修复并发布：新增红绿回归测试覆盖 public media URL 重写、无鉴权读取实例 media 文件、文件卡片和预览 URL 不再给 public media 添加 `auth_token`、`application/octet-stream + hello.html` 的 HTML 预览路由、刷新历史保留 outbound media 消息、刷新历史保留 snake_case `tool_events` 工具调用、schema v3 `fileEdits` 映射为 `write_file` 工具调用、带 media 的 assistant 交付补出 `message` 工具记录、上游投影跨 `_channel_delivery` 重建 `assistant.tool_calls` + `role: tool` 成对工具调用、`message` 工具保留工具记录，以及工作空间从 upstream thread media 返回交付文件；真实 `websocket_c9842da7-eb37-4f62-bb4c-24fd05bff56f.jsonl` 探针确认 `read_file`、`write_file`、`message` 三个工具记录都有 result；`./scripts/publish-local.sh` 通过，包含 Python 149 passed、前端 108 passed、构建和编译；`nanobot-webui` 和 `nanobot-webui-business` 均指向 `~/.local/share/uv/tools/nanobot-ai/bin/`；已重启 `nanobot-webui gateway` 和用户实例 `nanobot gateway`，`curl http://127.0.0.1:8081/health` 返回 `{"status":"ok","mode":"control_plane"}`。 |
| FIX-037 | P1 | 已修复 | HR business CLI 是智能体事实来源，但当前 `contract-expiry` 到期分析会把已被续签覆盖的旧合同返回为风险项；同时 PostgREST fallback adapter 对同一字段多个过滤条件可能互相覆盖，需要审计其它 CLI 分析/查询语义，避免智能体拿到错误业务事实。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/supabase_client.py`、`tests/test_business_cli.py` | `contract-expiry` 只返回当前有效合同的未来到期风险，不返回已续签旧合同或历史过期合同；PostgREST fallback adapter 对同字段 `gte/lte` 等多条件过滤保持完整；HR business CLI 高风险查询/分析命令有语义审计和必要回归测试。 | 已修复并发布：新增红绿测试覆盖 PostgREST 同字段多条件过滤和按员工当前合同判断到期风险；`tests/test_business_cli.py` 16 passed，`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 81 passed，`./scripts/publish-local.sh` 通过，包含 Python 149 passed、前端 108 passed、构建和编译；已重启控制面和用户实例，`curl http://127.0.0.1:8081/health` 返回 `control_plane`；安装后真实 CLI 探针显示 `business analyze contract-expiry --days 365` 只返回叶晨露和叶紫瑄，余慧娟不再误报，同时 `business query employee-contracts --name 余慧娟` 显示 sequence 2 续签至 2029-07-31。 |
| FIX-038 | P1 | 已修复 | 服务器部署后，浏览器从 `/api/upstream/bootstrap` 拿到用户实例内部地址 `ws://127.0.0.1:<port>/`，远端浏览器会连接自己本机的 127.0.0.1，导致 upstream websocket 连接失败。 | `src/nanobot_channel_webui/channel.py`、`src/nanobot_channel_webui/auth.py`、`frontend/src/ws-client.ts`、`tests/test_channel_managed_instances.py`、`tests/test_upstream_only_cleanup.py` | 托管实例仍只在服务器本机监听 `127.0.0.1`；浏览器拿到控制面同源 `/api/upstream/ws/{instance_id}` 地址；控制面鉴权后代理到实例 websocket sidecar；公网不暴露用户实例端口。 | 已修复并发布：新增控制面 WebSocket 代理 `/api/upstream/ws/{instance_id}`，bootstrap 返回同源代理路径；前端对同源 WS 自动附带 `webui_token` 并把 HTTP/HTTPS URL 归一化为 WS/WSS；后端校验当前账号只能访问自己的实例。验证：`tests/test_channel_managed_instances.py tests/test_upstream_only_cleanup.py` 21 passed；`./scripts/publish-local.sh` 通过，包含 Python 151 passed、前端 108 passed、构建和编译。 |
| FIX-039 | P1 | 已修复 | HR business CLI 创建奖惩记录时，`incident_date` 写入 `date[]` 字段未正规化为数组；改传数组后 PostgREST fallback 又把数组过滤序列化为 `eq.['2026-05-09']`，导致 create 后 verification 400，并诱导智能体反复尝试创建脏数据。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/supabase_client.py`、`tests/test_business_cli.py` | `business create disciplinary` 使用单个 `incident_date` 时能写入 `incident_dates date[]` 并完成 verification；PostgREST fallback 对数组 equality 使用合法 Postgres array literal；失败时不再因验证 URL 错误导致重复创建。 | 已修复并发布：`disciplinary_record_payload` 将单个 `incident_date` 正规化为 `incident_dates` 数组，PostgREST fallback 将数组过滤序列化为 `{...}`；新增回归测试覆盖 payload 和数组过滤；`tests/test_business_cli.py` 18 passed；`./scripts/publish-local.sh` 通过，包含 Python 153 passed、前端 108 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`；安装环境探针确认新序列化生效。田魏荣过失单流程中已产生一条 `incident_date=null` 的不完整记录，需另行按业务确认清理。 |
| FIX-040 | P1 | 已修复 | 页面运行中登录态失效后，API 返回 401 只在当前操作位置显示错误提示，没有清理前端登录态或自动展示登录入口，用户无法自然重新登录。 | `frontend/src/api.ts`、`frontend/src/ws-client.ts`、`frontend/src/use-auth-session.ts`、`frontend/src/store.workspace.test.ts`、`frontend/src/use-auth-session.test.ts` | 所有受控 API 和 upstream WebSocket bootstrap 遇到 401 时触发统一登录失效事件；前端清除本地 token、清空会话列表并显示 Supabase 登录页或 token 登录框；普通非 401 错误仍保留原错误提示。 | 已修复并发布：新增 `AuthExpiredError` 和全局 `nanobot:auth-expired` 事件，受控 API 与 upstream bootstrap 统一通过 `ensureOk` 处理 401；`useAuthSession` 监听事件后清 token、清会话并显示登录入口；定向前端测试 18 passed；`./scripts/publish-local.sh` 通过，包含 Python 153 passed、前端 110 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`。 |
| FIX-041 | P1 | 已修复 | 用户在对话中上传图片后，消息内图片缩略图仍使用旧 `/media/{signed-token}` URL，assistant-ui `<img>` 不会自动附带鉴权，导致控制台 401 且图片显示为碎片；点击预览走 public media 链路所以可打开。 | `src/nanobot_channel_webui/channel.py`、`tests/test_channel_managed_instances.py` | 上传接口返回的图片展示 URL 使用无需登录 token 的 `/api/public-media/{payload}`；public media 只允许解析当前控制面或托管实例 workspace 下 `.nanobot_webui_uploads` 内的上传文件，不暴露任意绝对路径；对话内图片和点击预览使用同一可访问 URL。 | 已修复并发布：上传响应 URL 改为 `/api/public-media/{payload}`；public media resolver 支持受限解析上传文件绝对路径，仅允许 `.nanobot_webui_uploads` 下的控制面或托管实例 workspace 文件；拒绝其它绝对路径；定向测试 34 passed；`./scripts/publish-local.sh` 通过，包含 Python 155 passed、前端 110 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`。 |
| FIX-042 | P1 | 已修复 | 对话流式输出或 upstream websocket 重连期间，若上游 `ready/attached` 事件缺少有效 `chat_id`，前端会把空 id 映射为 `session.init`，导致 `currentChatId` 被清空并切到新对话初始页；新对话带附件时还会在上传前先创建 upstream 线程 A，真正发送时又创建线程 B，导致历史里多出一个空“新对话”，并触发 assistant-ui runtime/线程列表状态跳变；第二轮带工具调用时，`session_updated` 触发会话列表刷新，若 upstream sessions 暂时漏掉当前会话，前端线程列表会退化出一个临时“新对话”行；上游 sessions 的 `preview` 还会把附件桥接文本 `[file: ...] [File: source: ...]` 当标题返回，使带附件会话看起来像跳到了新会话或被第二句重命名。 | `frontend/src/use-websocket-session.ts`、`frontend/src/use-webui-runtime.ts`、`frontend/src/store.ts`、`src/nanobot_channel_webui/channel.py`、`frontend/src/store-thread-list.test.ts`、`frontend/src/use-websocket-session.test.ts`、`frontend/src/thread-switch-source.test.ts`、`tests/test_channel_managed_instances.py` | 不完整的 `ready/attached` 事件不会切换当前会话、不会写入空 `chat_id` 到本地状态；附件上传不创建 upstream 会话，只落到当前用户 workspace 的草稿上传目录；真正发送消息时只创建一次 upstream 会话；本地用户消息提交时一次性切到真实线程；reducer 对空 `session.init/session.history` 做防御；会话列表刷新暂时缺当前会话时，已有本地消息或正在响应的当前会话会被保留并重建摘要，不退化成“新对话”；带附件发送和流式输出中的当前会话保持不变，历史不再新增空“新对话”；上游附件桥接 preview 统一净化为文件名，后续用户消息不覆盖已有附件会话标题。 | 已修复并发布：`normalizeServerEvent` 忽略空 `ready/attached`，附件 adapter 使用草稿上传 ID 而不是 `ensureThread()`，上传接口允许草稿 ID 但不放宽其它会话 API，store 防御空 `session.init/session.history` 并让 `local.user_message` 原子切入真实线程；`sessions.loaded` 会保留已有本地消息或进行中 turn 的当前会话摘要并净化附件桥接 preview；`local.user_message` 只在新会话首条消息生成标题，后续消息不再覆盖已有标题；`./scripts/publish-local.sh` 通过，包含 Python 156 passed、前端 121 passed、构建和编译；已重启 `nanobot-webui gateway`，`curl http://127.0.0.1:8081/health` 返回 `control_plane`；浏览器用 `liu.lifang@lechaoli.com` 和下载目录图片复测：刷新历史显示文件名标题，首轮带图和第二句“你又在瞎说了”完成后仍停留同一会话且标题保持附件文件名。 |
| FIX-043 | P1 | 已修复 | HR business update CLI 仍用手写字段白名单和匹配字段复用更新字段，导致部分表字段不能更新、`null` 清空被过滤、同一员工同日期异动记录无法用 `change_reason` 等字段唯一匹配；真库实测又发现 normalizer 会把缺省字段补成 `None` 导致未传字段被误清空，更新匹配字段后 verify 仍按旧值查找，列表字段 diff 会因 `list` 不可哈希崩溃，`disciplinary_records.incident_dates date[]` 查询用字符串匹配失败，`seal_usage` select 缺少原始外键导致 verify 误判。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/supabase_client.py`、`tests/test_business_cli.py` | 所有支持 create/update 的 HR 资源在真库上都能完成 `preview -> confirm/update -> verify`；匹配旧记录的字段不阻止同名字段被更新，必要时可用 `match_<field>` 提供旧值；显式 `null` 能清空可空字段，缺省字段不被误清空；数组、外键和更新后的匹配字段在 preview、update 和 verify 中使用同一字段口径。 | 已修复并发布：新增红绿回归测试覆盖员工 `resignation_date` 更新、`notes: null` 清空、人事异动用 `change_reason` 匹配同日同岗位重复记录并清空薪资字段、组织更新覆盖公司 `name/short_name` 和部门 `name/company_id`、合同更新匹配字段后 verify、保险 `signed_upload` 数组 diff、奖惩 `incident_dates date[]` 创建和更新、用章原始外键 select；通用 update 字段改为资源级完整可写字段常量，normalizer 保留字段缺省语义，verify 使用更新后的目标匹配值，列表比较类型安全，奖惩数组查询使用 Postgres array literal，用章 select 补齐 `company_id/applicant_id/seal_applicant_id`；真库实测目录 `/tmp/nanobot-hr-cli-e2e-20260610195318`，已完成 company/department、employee、contract、performance、insurance、personnel_change、disciplinary、seal_usage 的 create 和 update，最终 `employee-timeline` 返回 contracts=1、performance_reviews=1、insurance_changes=1、personnel_changes=1、disciplinary_records=2；按用户要求尚未删除测试数据。验证：`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 90 passed；定向 ruff 通过；`./scripts/publish-local.sh` 通过，包含 Python 163 passed、前端 121 passed、构建和编译；已重启 `nanobot-webui gateway`，健康检查返回 `{"status":"ok","mode":"control_plane"}`。 |
| FIX-044 | P1 | 已修复 | 数据库已支持 `is_deleted` 逻辑删除后，HR business CLI 查询、详情、分析、更新匹配和验证仍直接读取业务表，可能把逻辑删除数据当作正常业务事实返回给智能体。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`tests/test_business_cli.py`、`docs/REQUIREMENTS.md` | 所有默认业务查询和分析都排除 `is_deleted = true`；已删除数据只面向后续审计或显式管理入口，不进入普通业务事实。 | 已修复并发布：repository 表访问新增默认 active 记录代理，所有业务表 `select` 默认追加 `is_deleted = false`，并保留 `table_including_deleted()` 供后续审计入口使用；新增红绿测试覆盖员工列表和员工时间线子记录不返回逻辑删除数据；`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 92 passed；定向 ruff 通过；`./scripts/publish-local.sh` 通过，包含 Python 165 passed、前端 121 passed、构建和编译；已重启 `nanobot-webui gateway`，健康检查返回 `{"status":"ok","mode":"control_plane"}`。 |
| FIX-045 | P1 | 已修复 | 按智能体实际公开流程全量实测 HR CLI 时，`business preview organization --input <records.json>` 不接受其它资源一致的 `{records:[...]}` plan 包装；`business delete employee` 虽然默认查询不可见，但实际执行物理删除，导致逻辑删除数据无法用于审计追溯。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`tests/test_business_cli.py` | 智能体按公开 help 生成的组织录入 plan 可以使用 `records` 包装；员工删除设置 `is_deleted = true` 并写入 `updated_by`，默认业务查询不可见，但审计口径仍可查到记录。 | 已修复并发布：`normalize_org_plan` 兼容 `{records:[...]}`；`delete_employee_records` 改为逻辑删除；新增红绿测试覆盖组织 records 包装和员工删除审计保留；`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 94 passed；`./scripts/publish-local.sh` 通过，包含 Python 167 passed、前端 121 passed、构建和编译；安装后的真实 CLI 全流程 `/tmp/nanobot-hr-cli-full-20260610214739` 共 76 步全部符合预期，覆盖 help、query、get、analyze、preview、create、verify、preview-update、update、delete；删除后默认 `get employee` 返回 0 match，`table_including_deleted('employees')` 审计查询确认该员工仍存在且 `is_deleted=true`、`updated_by=39bcb5c4-f648-4bb5-a805-19916effba4e`；已重启 `nanobot-webui gateway`，健康检查返回 `{"status":"ok","mode":"control_plane"}`。 |
| FIX-046 | P1 | 已修复 | 智能体按公开 CLI 查询员工花名册明细时，`business query employee` 只返回 summary 字段，导致模型再逐个调用 `business get employee` 补全信息；同时逻辑删除只覆盖员工，合同、绩效、社医保、人事异动、奖惩和用章缺少公开删除与审计入口。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-query-analysis-router/`、`tests/test_business_cli.py` | `business query employee` 返回完整员工主档字段；公开 `help`、`capabilities` 和技能契约列出审计查询与业务记录逻辑删除命令；合同、绩效、社医保、人事异动、奖惩和用章删除设置 `is_deleted = true`，默认业务查询不可见，`business query deleted-records --resource <resource>` 可显式审计。 | 已修复并发布：新增红绿测试覆盖 help/capabilities、员工花名册完整字段、合同逻辑删除和 deleted-records 审计查询；`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 98 passed；`./scripts/publish-local.sh` 通过，包含 Python 171 passed、前端 121 passed、构建和编译；已重启 `nanobot-webui gateway`，健康检查返回 `{"status":"ok","mode":"control_plane"}`；安装后真 CLI 探针确认 `business capabilities` 暴露 `business delete contract --input <plan.json>` 和 `business query deleted-records --resource <resource>`，`business query employee --company 武汉未来天空音乐文化产业有限公司` 返回 12 条且首条包含 `birth_date/id_card_expiry/bank_account/bank_name/notes` 等完整主档字段；`business query deleted-records --resource employee --company 武汉未来天空音乐文化产业有限公司` 返回审计结构正常。 |
| FIX-047 | P1 | 已修复 | 普通 scoped 账号真实创建用章记录时，repository 准备后的 plan 包含 `match.company.company` 内部匹配对象，scope 提取函数把该 dict 字符串化后当作公司名校验，导致授权公司内的写入被误判越权。 | `src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`tests/test_business_cli.py` | scope 提取只采集标量公司字段，不把内部匹配对象、嵌套 dict 或 list 当作公司名；普通账号在授权公司范围内可以完成用章 create/update。 | 已修复并发布：新增红绿回归测试 `test_collect_scope_company_names_ignores_internal_match_company_objects`；`tests/test_business_cli.py` 34 passed，相关组合 `99 passed`；`./scripts/publish-local.sh` 通过，包含 Python 172 passed、前端 121 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`；普通账号 `guo.juan@lechaoli.com` 全矩阵实测 `/tmp/nanobot-scoped-cli-probe-guo.json` 共 53 步全部通过，覆盖只读查询、分析、写入预览、越权拒绝和当时的删除拒绝；真实写入实测 `/tmp/nanobot-scoped-cli-write-probe-guo.json` 共 25 步全部通过，覆盖员工、合同、绩效、社医保、人事异动、奖惩、用章的 create/update，测试数据已由 admin 逻辑删除清理且审计入口确认 `is_deleted=true`。 |
| FIX-048 | P1 | 已修复 | 普通 HR 账号已经具备 `delete` action，但 HR CLI 又额外把所有 scoped delete 命令列为危险命令，导致授权公司范围内也不能执行逻辑删除。 | `src/nanobot_channel_webui/business_modules/hr/runtime/policy.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/tenant-runtime.json`、`tests/test_business_cli.py` | 普通账号在具备对应 `hr.<resource>:delete` 且公司在 scope 内时，可以执行员工、合同、绩效、社医保、人事异动、奖惩和用章逻辑删除；未授权公司仍拒绝；`clear-business-data` 等全局破坏命令仍拒绝。 | 已修复并发布：删除 scoped 账号的一刀切 delete 拒绝，改为按 `resource:delete` 和公司 scope 授权；repository 删除兜底校验使用 `delete` action；技能契约移除逻辑删除命令的 denied 标记，并补充普通账号可执行授权范围内逻辑删除的说明；新增回归测试覆盖 scoped 用户可删除授权合同、不可删除未授权公司合同。验证：`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 101 passed；`./scripts/publish-local.sh` 通过，包含 Python 174 passed、前端 121 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`；普通账号 `guo.juan@lechaoli.com` 真库探针 `/tmp/nanobot-scoped-cli-delete-probe-guo.json` 共 16 步全部通过，覆盖员工、合同、绩效、社医保、人事异动、奖惩、用章由普通账号创建后逻辑删除，默认 `get employee` 返回 0 match，审计查询确认该员工 `is_deleted=true`。 |
| FIX-049 | P1 | 已修复 | 自然语言创建绩效、社医保异动、人事异动和奖惩记录时，模型会在 plan 中带上 `department` 等辅助匹配字段，通用 `generic_payload` 将这些非表字段写入 Supabase，导致 create 400；处分记录还会把不存在的 `notes` 列写入 `disciplinary_records`；公开 help 没有列出这些子资源的 `preview/create` 命令，和实际路由不一致；删除计划里模型常写 `employee`，但 CLI 只认 `name/employee_name`；新增和更新流程边界不够硬，容易让模型用 `preview-update` 测新建记录。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_business_cli.py`、`tests/test_hr_skill_routing_docs.py` | 子资源 create 只写入真实业务字段，不把 `company/department/name` 等匹配辅助字段写入数据库；处分 payload 只写入真实表字段；help、capabilities 和实际路由一致列出合同、绩效、社医保、人事异动、奖惩、用章的 `preview/create`；`employee` 可作为员工名别名；CLI help、技能说明和 tenant-runtime 明确新增必须 `preview/create`，修改已有记录才用 `preview-update/update`；本地自然语言验收能创建、回读并逻辑删除这些记录。 | 已修复并发布：新增回归测试覆盖通用子记录 payload 过滤、处分 payload 白名单、子资源 create help 暴露、`employee` 别名归一，以及新增/更新流程分流文案；`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 104 passed；`./scripts/publish-local.sh` 通过，包含 Python 178 passed、前端 121 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`；浏览器本地自然语言复测确认员工创建/详情/更新、合同/绩效/社医保/人事异动/奖惩/用章创建回读、普通账号逻辑删除和 deleted-records 审计入口均通过，测试数据已逻辑删除。 |
| FIX-050 | P1 | 已修复 | 纯自然语言实测员工主档更新时，`preview-update employee` / `update employee` 的匹配语义对“新值”和“旧值”区分不清，模型多次尝试后仍无法完成岗位和手机号更新；更严重的是，模型在用户只确认“保存修改”的情况下自行尝试 `delete employee` 作为删除重建降级路径，虽然因输入格式错误未落库，但写操作安全边界不符合 HR 真实使用预期；复测时还发现员工 create 内部 verification 对默认 `status=正式` 的比较口径不一致。 | `src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_business_cli.py`、`tests/test_hr_skill_routing_docs.py` | 员工主档更新支持稳定的自然语言计划格式，例如用姓名、公司、原手机号或 `match_*` 字段匹配旧记录，用目标字段表达新值；`preview-update` 和 `update` 使用一致匹配口径；当更新失败时，模型只能向用户报告失败或请求明确下一步，不能把“更新确认”解释为删除重建授权；删除重建必须要求用户单独明确确认；员工 create 内部 verification 使用和写入一致的默认字段口径。 | 已修复：员工更新改用独立匹配逻辑，默认姓名+公司匹配旧员工，显式 `match_phone` / `match_id_card_number` 可匹配旧手机号或旧证件号，目标 `phone` / `id_card_number` 只作为更新值；员工 create verification 改用 `employee_payload` 同一映射口径；`hr-db-ops` 技能说明和 tenant-runtime 明确更新失败不能自动删除重建。验证：新增回归测试覆盖改手机号时的 preview/update/verification、旧手机号匹配和默认状态 create verification，契约测试覆盖禁止删除重建；浏览器中文自然语言复测会话 `websocket_41c3e492-7a35-49bc-b274-9090ddb236f1.jsonl` 确认 create 后 update 命中 `preview-update employee` 与 `update employee`，verification.ok=true，未出现 delete fallback，测试员工已逻辑删除清理。 |
| FIX-051 | P2 | 已修复 | 纯自然语言写入和回读流程仍存在 CLI 契约示例不够强的问题：员工新增时模型先后尝试了多种无效 JSON plan 结构才找到正确扁平结构；子资源回读时模型尝试了不存在的 `business query contract-expiry`，失败后才改用 `business analyze contract-expiry`。这些不会直接造成错误结果，但会拉长 HR 用户等待时间并增加模型绕路概率；同时 create/update/delete 已内置验证，模型不应在成功写入后自动二次 get/query/analyze 回读。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_hr_skill_routing_docs.py` | 技能说明和 tenant-runtime 对写入 plan 提供模型可直接照抄的最小 JSON 示例；公开契约明确只写一次 JSON plan，失败时最多按错误修改同一文件一次；create/update/delete 成功结果足够作为验证依据，除非用户明确要求回读，不再自动运行 get/query/analyze 二次验证。 | 已修复：`hr-db-ops` 技能说明和 `tenant-runtime.json` 明确单 plan 文件、扁平对象或 records 数组、内部 verification 收尾规则；新增契约测试覆盖“不额外回读”和“最小 plan 示例”。验证：定向测试 `2 passed`。 |
| FIX-052 | P1 | 已修复 | 纯自然语言 create/update 流程中，模型可能先根据用户原始文本口头列出字段并请求确认，等用户确认后才写 JSON plan、运行 preview，并紧接着 create/update；这和设计意图不符。正确流程应是先生成 JSON plan，先运行 `business preview` 或 `business preview-update`，把 CLI preview 的实际结果转换为业务语言给用户确认，用户确认后才执行 create/update，且 create/update 内部用同一个 JSON plan 做写入后校验。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`tests/test_hr_skill_routing_docs.py` | 写入类技能契约明确禁止基于原始用户文本直接请求确认；确认前必须已有 JSON plan 和成功的 CLI preview 结果；模型必须展示 preview 返回的 records、matched、skipped、diffs 或风险摘要；create/update 只能在用户确认后执行，并继续使用同一 plan 进行内部 verification；用户可见回复不暴露 `CLI`、`preview`、`预览`、`系统预览` 等内部术语。 | 已修复：`hr-db-ops` 明确内部 preview 先于确认、用户可见只说“拟录入信息/拟更新信息”，并要求姓名按原文完整保留；`tenant-runtime.json` 的 create/update recipe 明确确认必须发生在 JSON plan 和 preview 成功之后。验证：新增契约测试覆盖内部 preview 顺序、用户可见禁词和姓名完整性；`tests/test_business_cli.py tests/test_permissions.py tests/test_hr_skill_routing_docs.py` 112 passed；`./scripts/publish-local.sh` 通过，包含 Python 185 passed、前端 121 passed、构建和编译；已重启控制面，健康检查返回 `control_plane`；浏览器普通自然语言复测确认 create 流程为 `write_file` → `business preview employee` → 用户确认 → `business create employee` 且 create 验证通过，update 流程为 `write_file` → `business preview-update employee` → 用户确认 → `business update employee` 且 update 验证通过；测试员工已逻辑删除清理。 |
| FIX-053 | P1 | 已修复 | 普通账号自然语言多资源写入时，单资源流程已隐藏内部术语，但多资源确认流程仍会向用户暴露“预览”“所有预览都通过”“Now let me run the preview”等内部措辞。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/tenant-runtime.json`、动态技能视图 | 多资源 create/update 也只能向用户展示“拟录入信息 / 拟更新信息”，不能出现 `CLI`、`preview`、`预览`、`系统预览` 等内部词；浏览器普通账号自然语言多资源录入确认页不暴露内部术语。 | 已修复：`hr-db-ops` 和 `tenant-runtime.json` 明确多资源流程也不能向用户暴露内部 preview/CLI 术语；浏览器普通账号自然语言复测确认摘要使用“以下拟录入信息已整理完成，请确认”，未再出现“所有预览都通过了”或英文 preview 过程。验证：`tests/test_hr_skill_routing_docs.py` 覆盖契约；`./scripts/publish-local.sh` 通过并已重启，健康检查返回 `control_plane`。 |
| FIX-054 | P1 | 已修复 | 绩效 create 在多资源自然语言录入中第一次生成的 plan 缺少 `review_date` / `self_score` / `supervisor_score`，确认后 create 失败才读取 schema 并多次改写同一个 plan；这违背“确认前计划完整、确认后只执行同一 plan”的语义。 | `src/nanobot_channel_webui/business_modules/hr/skills/hr-db-ops/`、`src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py` | 绩效自然语言 create 在确认前就能生成可直接写入并验证通过的 plan；确认后不再因为字段缺失重新查询 schema、改写 plan 或重复 create。 | 已修复：绩效 plan 归一化支持 `review_month/month -> review_date`，并在只有 `final_score` 时补齐 `self_score/supervisor_score`；浏览器普通账号多资源 create 复测中 `review_month=2026-06` 直接 create 成功且内部验证通过。验证：新增红绿测试 `test_performance_create_normalizes_natural_language_month_plan`；`./scripts/publish-local.sh` 通过。 |
| FIX-055 | P1 | 已修复 | 人事异动 create 可以成功，但后续普通自然语言 update 无法稳定通过 `preview-update personnel-change` 匹配同一条记录，模型尝试多种 match 字段仍失败，只能跳过更新。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`tests/test_business_cli.py` | 人事异动 update 支持用员工、公司、生效日期、原岗位/新岗位、异动原因等自然语言字段稳定匹配已创建记录；preview-update、update 和 verification 使用一致匹配口径。 | 已修复：新增人事异动归一化支持 `change_date/change_type`，匹配逻辑先按完整条件查找，找不到时按稳定字段和显式 match 字段回退，不再把新岗位/新原因误当旧值强制匹配；浏览器复测确认摘要能显示新岗位和异动原因 diff。验证：新增红绿测试 `test_personnel_change_update_matches_created_record_when_changing_reason`；真实安装 CLI 探针确认 preview-update would_update、update verification.ok=true；测试数据已逻辑删除。 |
| FIX-056 | P1 | 已修复 | 用章记录 update 对附件更新成功，但用途/事由字段没有按自然语言“用途改成...”落库；模型确认后报告“附件已改，用途未改”。 | `src/nanobot_channel_webui/business_modules/hr/runtime/commands.py`、`src/nanobot_channel_webui/business_modules/hr/runtime/repository.py`、`tests/test_business_cli.py` | 用章 update 支持自然语言中的“用途/事由/reason”字段变更，preview-update 能显示 diff，update 后 verification 确认用途已更新。 | 已修复：用章归一化支持 `new_reason/current_reason/old_reason` 和 `seal_applicant_name/applicant_name`；update/delete 缺省用章人时不再强制匹配 `seal_applicant_id is null`，可按公司、日期、旧用途唯一匹配；真实安装 CLI 探针确认 `current_reason -> new_reason` 产生 reason 与附件 diff，update verification.ok=true，随后逻辑删除并默认员工查询返回 0。验证：新增红绿测试 `test_seal_usage_update_supports_new_reason_alias`；`./scripts/publish-local.sh` 通过并已重启。 |

## 维护规则

- 新发现 bug、评审意见或安全风险时，先新增或更新本文件。
- 处理前将状态改为 `进行中`。
- 修复完成后改为 `已修复`，并在备注中记录验证方式、发布时间或关联提交。
- 如果需求发生变化，应标记 `延后` 或 `取消`，不要直接删除历史问题。
